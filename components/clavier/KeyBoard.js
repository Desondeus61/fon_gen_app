export default function FonKeyBoard({onKeyup}) {
  return (
    <div className="my-3">
      <div className="left-side-btn dropup">
        <button
        onClick={()=>{
            if(onKeyup){
                onKeyup('à');   
            }
        }}
          className="react-btn btn-default btn-small btn-border"
        >
          à
        </button>
        <button
        onClick={()=>{
            if(onKeyup){
                onKeyup('á');   
            }
        }}
        className="react-btn btn-default btn-small btn-border">
          á
        </button>
        <button
        onClick={()=>{
            if(onKeyup){
                onKeyup('ā');   
            }
        }}
        className="react-btn btn-default btn-small btn-border">
          ā
        </button>
        <button
        onClick={()=>{
            if(onKeyup){
                onKeyup('ǎ');   
            }
        }}
        className="react-btn btn-default btn-small btn-border">
          ǎ
        </button>

        <button
        onClick={()=>{
            if(onKeyup){
                onKeyup('ɔ');   
            }
        }}
        className="react-btn btn-default btn-small btn-border">
          ɔ
        </button>
        <button
        onClick={()=>{
            if(onKeyup){
                onKeyup('ɔ̀');   
            }
        }}
        className="react-btn btn-default btn-small btn-border">
          ɔ̀
        </button>
        <button
        onClick={()=>{
            if(onKeyup){
                onKeyup('ɔ́');   
            }
        }}
        className="react-btn btn-default btn-small btn-border">
          ɔ́
        </button>
        <button
        onClick={()=>{
            if(onKeyup){
                onKeyup('ɔ̄');   
            }
        }}
        className="react-btn btn-default btn-small btn-border">
          ɔ̄
        </button>
        <button
        onClick={()=>{
            if(onKeyup){
                onKeyup('ɔ̌');   
            }
        }}
        className="react-btn btn-default btn-small btn-border">
          ɔ̌
        </button>

        <button
        onClick={()=>{
            if(onKeyup){
                onKeyup('ɛ');   
            }
        }}
        className="react-btn btn-default btn-small btn-border">
          ɛ
        </button>
        <button
        onClick={()=>{
            if(onKeyup){
                onKeyup('ɛ̀');   
            }
        }}
        className="react-btn btn-default btn-small btn-border">
          ɛ̀
        </button>
        <button
        onClick={()=>{
            if(onKeyup){
                onKeyup('ɛ́');   
            }
        }}
        className="react-btn btn-default btn-small btn-border">
          ɛ́
        </button>
        <button
        onClick={()=>{
            if(onKeyup){
                onKeyup('ɛ̄');   
            }
        }}
        className="react-btn btn-default btn-small btn-border">
          ɛ̄
        </button>
        <button
        onClick={()=>{
            if(onKeyup){
                onKeyup('ɛ̌');   
            }
        }}
        className="react-btn btn-default btn-small btn-border">
          ɛ̌
        </button>

        <button
        onClick={()=>{
            if(onKeyup){
                onKeyup('ò');   
            }
        }}
        className="react-btn btn-default btn-small btn-border">
          ò
        </button>
        <button
        onClick={()=>{
            if(onKeyup){
                onKeyup('ó');   
            }
        }}
          className="react-btn btn-default btn-small btn-border"
        >
          ó
        </button>
        <button
        onClick={()=>{
            if(onKeyup){
                onKeyup('ǒ');   
            }
        }}
        className="react-btn btn-default btn-small btn-border">
          ǒ
        </button>
        <button
        onClick={()=>{
            if(onKeyup){
                onKeyup('ō');   
            }
        }}
        className="react-btn btn-default btn-small btn-border">
          ō
        </button>

        <button
        onClick={()=>{
            if(onKeyup){
                onKeyup('ć');   
            }
        }}
        className="react-btn btn-default btn-small btn-border">
          ć
        </button>
        <button
        onClick={()=>{
            if(onKeyup){
                onKeyup('č');   
            }
        }}
        className="react-btn btn-default btn-small btn-border">
          č
        </button>

        <button 
        onClick={()=>{
            if(onKeyup){
                onKeyup('ɖ');   
            }
        }}
        className="react-btn btn-default btn-small btn-border">
          ɖ
        </button>

        <button
        onClick={()=>{
            if(onKeyup){
                onKeyup('ì');   
            }
        }}
        className="react-btn btn-default btn-small btn-border">
          ì
        </button>
        <button
        onClick={()=>{
            if(onKeyup){
                onKeyup('í');   
            }
        }}
        className="react-btn btn-default btn-small btn-border">
          í
        </button>
        <button
        onClick={()=>{
            if(onKeyup){
                onKeyup('ǐ');   
            }
        }}
        className="react-btn btn-default btn-small btn-border">
          ǐ
        </button>
        <button
        onClick={()=>{
            if(onKeyup){
                onKeyup('ī');   
            }
        }}
        className="react-btn btn-default btn-small btn-border">
          ī
        </button>

        <button
        onClick={()=>{
            if(onKeyup){
                onKeyup('é');   
            }
        }}
        className="react-btn btn-default btn-small btn-border">
          é
        </button>
        <button
        onClick={()=>{
            if(onKeyup){
                onKeyup('è');   
            }
        }}
        className="react-btn btn-default btn-small btn-border">
          è
        </button>
        <button
        onClick={()=>{
            if(onKeyup){
                onKeyup('ě');   
            }
        }}
        className="react-btn btn-default btn-small btn-border">
          ě
        </button>
        <button
        onClick={()=>{
            if(onKeyup){
                onKeyup('ē');   
            }
        }}
        className="react-btn btn-default btn-small btn-border">
          ē
        </button>

        <button
        onClick={()=>{
            if(onKeyup){
                onKeyup('ŋ');   
            }
        }}
        className="react-btn btn-default btn-small btn-border">
          ŋ
        </button>
      </div>
    </div>
  );
}
